# sprinkled: .profile v1

# load aliases if available
if [ -f ~/.aliases ]; then
  source ~/.aliases
fi

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi
